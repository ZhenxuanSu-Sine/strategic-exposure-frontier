# Exact frontier skill

For finite strategy spaces enumerate exactly when feasible. Store reward and resource coordinates independently. Compute envelopes by feasibility filtering; do not assume concavity. If policy mixing is allowed, state it and compute convexification explicitly. For epsilon-best-response exposure, export all breakpoints and the response attaining each segment.
