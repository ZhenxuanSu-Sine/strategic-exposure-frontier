# Reproducibility skill

For every E23+ result record experiment ID, falsifiable question, exact parameters, random seeds, package/engine versions, hardware when runtime-sensitive, raw machine-readable output, summary JSON, and hashes. External engines require binary/version/option metadata. Preserve null results.
